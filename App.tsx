import {StyleSheet, Text, View} from 'react-native';
import React, {useEffect} from 'react';

const App = () => {
  useEffect(() => {});
  return (
    <View>
      <Text style={{fontFamily: 'Lemon-Regular'}}>App</Text>
    </View>
  );
};

export default App;

const styles = StyleSheet.create({});
